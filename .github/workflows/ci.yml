name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.93.0"
          components: rustfmt, clippy

      - uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Clippy (otel feature)
        run: cargo clippy -p tokf-cli --features otel -- -D warnings

      - name: Clippy (otel-grpc feature)
        run: cargo clippy -p tokf-cli --features otel-grpc -- -D warnings

      - name: Run tests
        run: cargo test --workspace

      - name: Run tests (otel feature)
        run: cargo test -p tokf-cli --features otel

      - name: Run tests (otel-grpc feature)
        run: cargo test -p tokf-cli --features otel-grpc

      - name: Verify filter test suites
        run: ./target/debug/tokf verify --require-all

      - name: Check file sizes
        run: bash scripts/check-file-sizes.sh
