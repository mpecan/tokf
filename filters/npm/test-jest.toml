command = "jest"
strip_ansi = true
skip = [
  "^\\s*$",
  "^> ",
  "^\\s*npm (warn|notice|WARN|verbose|info|timing|error|ERR)",
  "^\\s*WARN\\s",
  "^\\s*console\\.log",
  "^\\s*at Object",
]

[on_success]
output = "{output}"

[on_failure]
tail = 30
