# Works with both docker compose and podman compose.
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: tokf
      POSTGRES_PASSWORD: tokf
      POSTGRES_DB: tokf_dev
    ports: ["5432:5432"]
