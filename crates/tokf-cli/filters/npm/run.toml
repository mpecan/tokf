command = "npm run *"
description = "Strip npm lifecycle noise; show script output only"
skip = [
  "^> .+@",
  "^\\s*npm warn",
  "^\\s*npm notice",
  "^\\s*npm WARN",
  "^[⸩⸨]",
]

[on_success]
output = "{output}"

[on_failure]
tail = 20
