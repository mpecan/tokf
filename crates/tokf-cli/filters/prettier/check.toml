command = "prettier --check"
description = "Confirm ok or list files needing formatting"
strip_ansi = true
skip = [
  "^Checking formatting",
  "^\\s*$",
]

[on_success]
output = "ok ✓ prettier: all files formatted"

[on_failure]
output = "{output}"
