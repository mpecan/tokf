command = "go test"
description = "Strip per-test RUN/PASS noise; show failures and summary"
skip = ["^=== RUN ", "^=== PAUSE ", "^=== CONT ", "--- PASS:", "^PASS$", "^\\?\\s+"]

[on_success]
output = "✓ go test\n{output}"

[on_failure]
skip = ["^ok\\s+"]

[fallback]
tail = 20
