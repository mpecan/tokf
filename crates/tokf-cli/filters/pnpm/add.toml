command = "pnpm add *"
description = "Strip download progress; show added packages"
skip = [
  "^\\s*Progress:",
  "^\\s*Downloading",
  "^\\s*Already up to date",
]

[on_success]
output = "{output}"

[on_failure]
tail = 10
