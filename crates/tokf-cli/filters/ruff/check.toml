command = "ruff check"
description = "Strip blank lines; show lint errors with locations"
strip_ansi = true
skip = [
  "^\\s*$",
]

[on_success]
output = "ok ✓ ruff check: no issues"

[on_failure]
output = "{output}"
