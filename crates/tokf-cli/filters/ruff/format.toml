command = "ruff format"
description = "Confirm ok or list files needing formatting"
strip_ansi = true
skip = [
  "^\\s*$",
]

[on_success]
output = "ok ✓ ruff format: all files formatted"

[on_failure]
output = "{output}"
