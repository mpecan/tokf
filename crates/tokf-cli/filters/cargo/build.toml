command = "cargo build"
description = "Strip compile progress; show warnings and errors only"
skip = [
  "^   Compiling ",
  "^    Checking ",
  "^    Updating ",
  "^   Downloaded ",
  "^  Downloading ",
  "^     Locking ",
]

[on_success]
output = "ok ✓"

[on_failure]
tail = 30
