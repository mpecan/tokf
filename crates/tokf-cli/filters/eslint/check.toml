command = "eslint"
description = "Strip summary counts; show lint errors with file locations"
strip_ansi = true
skip = [
  "^\\s*$",
  "^\\s*✖",
]

[on_success]
output = "ok ✓ eslint: no issues"

[on_failure]
output = "{output}"
